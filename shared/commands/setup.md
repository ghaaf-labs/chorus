---
description: Refresh Chorus's capability registry (probe installed CLIs and their auth)
allowed-tools: Bash(chorus:*), Bash(node:*)
---

Refresh the Chorus capability registry. Probes whether claude, codex, grok, and opencode are installed and authenticated.

Run:

```bash
chorus setup
```

Return Chorus's stdout verbatim.
