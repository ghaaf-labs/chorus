---
description: Show Chorus job history with filters
argument-hint: [--source <name>] [--target <name>] [--role <name>] [--limit N]
allowed-tools: Bash(chorus:*), Bash(node:*)
---

Show filtered Chorus job history.

Raw slash-command arguments:
`$ARGUMENTS`

Run:

```bash
chorus history $ARGUMENTS
```

Return Chorus's stdout verbatim.
