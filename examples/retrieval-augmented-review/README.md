# Retrieval Augmented Review

Runs a reviewer call with `--retrieve` when `CHORUS_EXAMPLE_LIVE=1`. The script
skips if live mode is off or the local Knowledge Index target cannot be found.

```bash
./run.sh
```
