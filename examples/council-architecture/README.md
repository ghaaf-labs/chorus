# Council Architecture

Runs a three-target architecture council when `CHORUS_EXAMPLE_LIVE=1` and at
least three council targets are available. Otherwise it skips with a clear
message.

```bash
./run.sh
```
